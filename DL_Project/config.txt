[GENERAL]
    device = cpu
    seed = 0

[DATASET]
    choice = Single
    full_path = images\
    relative_path = images
    image_path = images\Truck\truck.jpg

[ATTACK]
    max_iter = 10
    max_strength = 255 #(can be in range [0,255])
    target = Missing Detection  #(Missing at least 1/All Detection, False Positive, IOU)


